# Car-price-prediction
A Flask-based car price prediction app that uses a trained scikit-learn model to estimate resale value from car company, model, year, fuel type, and kilometers driven, with a simple web interface and dynamic model selection.
